<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= render partial: 'shared/realcruiter_com_navbar', locals: { css_class: 'navbar-brand'} %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">

        <li class='active'>
           <%= link_to(:root) do %>
            <i class='fa fa-home'></i>&nbsp;Home
          <% end %>
        </li>
<%
=begin
%>
        <li>
          <%= link_to(new_profile_path) do %>
            <i class='fa fa-edit'></i>&nbsp;Create a Profile
          <% end %>
        </li>
<%
=end
%>
      </ul>

      <div class='container'>
        <%= form_for :user, url: user_session_path,
                            builder: InlineErrorsBuilder,
                            method: :POST,
                            html: { class: 'navbar-form navbar-right' },
                            role: :form do |f| %>
            <div class='form-group'>
              <%= f.email_field :email, class: 'form-control', placeholder: 'Email or username' %>
            </div>

            <div class='form-group'>
              <%= f.password_field :password, class: 'form-control', placeholder: 'Password' %>
            </div>  

            <div class='actions form-group'>
              <%= submit_tag 'Log In', class: 'btn btn-default _btn-special _dark' %>
            </div>
        <% end %>
      </div>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>