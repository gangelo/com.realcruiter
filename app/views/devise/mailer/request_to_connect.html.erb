<p>Greetings <%= @resource.email %>,</p>

<p>Congratulations!</p>

<p>Someone likes your "<%= @user_profile.profile_name %>" <%= @user_profile.type.titleize.downcase %>, and wants to connect with you!</p>

<p>You may view this request by clicking the below link:</p>

<p><%= link_to 'Click here to view the request', connections_request_url(uuid: @token) %></p>

<br/>
<strong>Having trouble?</strong>
<p>If for any reason the above link does not work, simply copy &amp; paste the below link into your browser's url:</p>

<p><%= connections_request_url(uuid: @token) %></p>